# Maps

The original `map.pgm` / `map.yaml` generated in the virtual-machine/robot environment are not currently available on this computer.

Do **not** fabricate them for the portfolio.

If historical map files are recovered later, place sanitized copies here, for example:

```text
maps/
├── lab_map.pgm
└── lab_map.yaml
```
